
function openSocialLink(){
    
    const links = document.querySelectorAll(link-btn);

    ElementInternals.array.forEach(link => {
        if(link === "github-btn"){
            console.log("Open GitHub")
        } else if (link === "Linkedin-btn"){
            console.log("Open Linkedin")
        }
    });
}